#include <stdio.h>

int main(){
    /**
     * Em C, não existe o tipo BOOLEAN
     * Qualquer valor numérico diferente de 0 (zero), o compilador reconhece como FALSE, o restante como TRUE
     */

    int lindo = 1;//verdadeiro
    float maravilhoso = 1.52;//verdadeiro
    double cagado = 0; //falso

    return 0;
}